# Fehleranalyseprotokoll

**Fundamentale Funktion für den Sportwagenmodus:** Ein Standard pro Problemstellung gilt auch bei Fehlern – eine verbindliche Ablauf (Protokoll lesen → vergangene Fehler prüfen → analysieren → fixen + absichern → eintragen), keine doppelten Fixes, Wiederholungen sofort erkennen und absichern. Ohne diese Funktion würden gleiche Fehlerklassen immer wieder neu „erfunden“ statt einmal gelöst und überall angewendet.

**Zweck:** Bei jeder Fehlermeldung von Georg wird dieses Protokoll genutzt und ergänzt. Es dient der Orientierung (welche Fehlerklassen gab es, welche Absicherungen gelten) und der Qualitätsentwicklung (Wiederholungen erkennen, Absicherungen verschärfen).

---

## Vergangene Fehler – Quellen (verbindlich bei Fehleranalyse)

**Bei jeder Fehlermeldung zuerst prüfen**, ob derselbe oder ein ähnlicher Fehler schon vorkam. Keine doppelten Fixes, Wiederholungen sofort erkennen.

| Quelle | Inhalt | Wann nutzen |
|--------|--------|-------------|
| **docs/GELOESTE-BUGS.md** | Alle behobenen Bugs (BUG-001 bis BUG-022+): Symptom, Ursache, Lösung, betroffene Dateien, Regeln. **PFLICHT bei Session-Start** und vor Änderungen an betroffenen Stellen. | Bei jeder Fehlermeldung: Durchsehen ob gleiche Symptomatik oder gleiche Ursachenklasse (z. B. stilles Überschreiben, Merge, Kontext, QR). |
| **docs/CRASH-BEREITS-GEPRUEFT.md** | Bereits geprüfte Crash-Ursachen, ro5, Code-5-Regeln. Bei neuem Crash **zuerst** lesen, nicht dieselben Stellen wieder durchgehen. | Bei Meldung zu Absturz/Code 5/Reopen. |
| **docs/ANALYSE-OEK2-GALERIE-BETRETEN-FEHLER-06-03.md** | Variable vor Verwendung (useLocation/Hooks am Komponentenanfang). | Bei „Cannot access uninitialized variable“ oder Hooks-Reihenfolge. |
| **docs/LEHRE-WERKE-WEG-IPAD-NOCH-DA.md** | Lehre: Werke weg am iPad, Merge/Server-Logik. | Bei Sync/„Werke verschwunden“. |
| **.cursor/rules/niemals-kundendaten-loeschen.mdc** | Absolutes Verbot: Filter + setItem, stilles Löschen, Überschreiben mit weniger Werken. | Bei jedem Thema Speichern/Laden/Merge. |
| **docs/BEKANNTE-LIMITATIONEN-AKZEPTIERT.md** | **Akzeptierte Limitationen** (ök2 zwei Werkbilder fehlen; alte Bilder am Handy). Dutzende Versuche, keine Lösung – **keine weiteren „vielleicht geht’s“-Versuche**. Professionell = dokumentiert, eingestellt. | Bei diesen Themen: hier lesen, **nicht** erneut probieren. Nur bei neuer konkreter Hypothese oder neuem Mittel mit Georg klären. |
| **docs/LEHRE-DESIGN-FEHLER-SERVER-WAHRHEIT.md** | **Design-Fehler Sync:** Merge ließ Lokal den Server überschreiben. Richtige Grundregel: Nach „An Server senden“ ist Server beim Abholen **einzige Wahrheit**. Lehre: Bei Sync/authoritative source **zuerst** Grundregel klären, dann bauen. | Bei Sync, „eine Quelle der Wahrheit“, Merge: Doku lesen; nie Lokal über Server beim expliziten „Stand holen“. |
| **docs/DATENFLUSS-GALERIE-STRUKTUR.md** | **Struktur und Datenfluss Galerie/Stammdaten:** Zwei Quellen (Stammdaten, Galerie gestalten/Texte), Keys pro Kontext (K2/ök2/VK2), wer liest/schreibt wohin. **Verbindlicher Ablauf bei Fehlersuche:** (1) Struktur und Datenfluss klären, (2) Ursache eingrenzen (Quelle vs. Anzeige), (3) erst dann an der Quelle reparieren. | Bei Fehlern zu Galerie-Daten, Anzeige falscher Inhalte, Kontext-K Vermischung: zuerst diese Doku + fehlersuche-zuerst-struktur-datenfluss.mdc – nie sofort an der Oberfläche reparieren. |
| **docs/BERICHT-CHAOS-NACH-GROESSEREN-AENDERUNGEN.md** | **Wo wir nach größeren Änderungen schon reparieren mussten:** Admin/Kontext, Sync/Merge, Bilder/Werke, Keys, Routen, Stand – Übersicht + Verweis auf Regeln. | **Bei jeder größeren Änderung** (Refactor, mehrere Bereiche, Aufräumen): Bericht lesen + eiserne-regel-groessere-aenderung-kein-chaos.mdc einhalten. |

**Regel:** Vor Analyse und Fix: Diese Quellen konsultieren → Wiederholung oder gleiches Muster? Dann bestehende Lösung/Regel anwenden und ggf. verschärfen, nicht neu erfinden. **Bei akzeptierten Limitationen:** Kein weiteres Probieren ohne klaren neuen Ansatz.

---

## Wie die KI es nutzt (verbindlich)

- **Bei jeder Fehlermeldung von Georg:** (1) **Vergangene Fehler** (oben) prüfen – GELOESTE-BUGS, ggf. CRASH-BEREITS-GEPRUEFT. (2) Dieses Protokoll lesen („Bekannte Fehlerklassen“, „Laufende Absicherungen“, letzte Einträge).
- **Nach jeder Fehleranalyse:** Eintrag unter „Protokoll-Einträge“ (Datum, Stichwort Meldung, Ursache, getroffene Maßnahme, ggf. „Wiederholung von BUG-XXX“).
- **Regressions-Check:** Beim Eintrag in GELOESTE-BUGS prüfen: Gibt es einen Test, der den Fix absichert (damit derselbe Fehler nicht wieder durchgeht)? Wenn **nein**: minimalen Test ergänzen (z. B. in datentrennung.test.ts, kritische-ablaeufe.test.ts oder passender Test-Datei). Siehe docs/QS-VERGLEICH-PROFIS.md „Regressions-Test nach Bugfix“.
- **Bei Wiederholung derselben Fehlerklasse:** Absicherung verschärfen (Regel/Checkliste), im Eintrag vermerken und in „Bekannte Fehlerklassen“ den Hinweis „Wiederholung – Absicherung verschärft am …“ ergänzen.

---

## Bekannte Fehlerklassen

*(Aus vergangenen Bugs abgeleitet – siehe GELOESTE-BUGS.md für Einzelfälle.)*

| Fehlerklasse | Kurzbeschreibung | Absicherung / Regel | Referenz (Vergangenheit) |
|-------------|------------------|---------------------|--------------------------|
| Stilles Überschreiben | Daten bei Fehler/Fallback durch alte oder leere Quelle ersetzt, ohne Meldung | „Vom Server laden“: Keine statische Datei bei API-Fehler; klare Fehlermeldung. GalerieVorschauPage handleRefresh. | Sync Mobil→Mac (10.03.26); BUG-018, BUG-021 |
| Kein sichtbares Feedback | Nutzer erfährt nicht, ob Aktion (z. B. Veröffentlichen) Erfolg oder Fehler hatte | Nach Speichern: setLoadStatus „✅ Veröffentlicht …“ oder „❌ Veröffentlichen fehlgeschlagen …“. | Sync Mobil→Mac; BUG-017 |
| Verschiedene Quellen Senden vs. Laden | Mobil sendet an andere Adresse als die, von der Mac lädt | publishGalleryDataToServer immer GALLERY_DATA_BASE_URL (Vercel). | Sync Mobil→Mac; BUG-018 |
| Filter + setItem / stilles Löschen | Gefilterte Liste wird in localStorage geschrieben → Werke gehen verloren | Niemals: filter + setItem. Regel: niemals-kundendaten-loeschen.mdc. | BUG-005, BUG-011, BUG-021; Supergau VK2 (23.02.26) |
| Merge ohne preserveLocalImageData | Server-Daten ersetzen lokale Bilddaten (imageRef/imageUrl) | Immer mergeServerWithLocal + preserveLocalImageData; syncMerge.ts. | BUG-021 |
| Lokale Werke nicht in merged | Lokale Werke ohne Server-Eintrag landen nicht in merged → beim Speichern weg | Merge: lokale Werke ohne Server-Eintrag immer in merged übernehmen (nicht nur „mobile + very new“). | BUG-012, BUG-013 |
| API vs. statische Datei (Reihenfolge) | Zuerst statische Datei geladen → alter Build-Stand | Immer zuerst GET /api/gallery-data (Blob), nur bei Fehler Fallback; GaleriePage loadData. | BUG-018 |
| Kontext-Vergiftung (sessionStorage) | Admin-Kontext bleibt hängen → K2 schreibt in ök2-Keys | syncAdminContextFromUrl; URL vor sessionStorage prüfen. | BUG-004 |
| QR/Stand falsche Quelle (Cache) | QR mit lokalem BUILD_TIMESTAMP → Handy bekommt alte Version | buildQrUrlWithBust + useQrVersionTimestamp; stand-qr-niemals-zurueck.mdc. | BUG-006 |
| Automatischer Reload (Code 5) | Reload bei „Server neuer“ → Loop → Crash | Kein automatischer Reload; nur Badge/Button. code-5-crash-kein-auto-reload.mdc. | BUG-007 |
| ök2 Willkommensbild Uraltbild | Default = Repo-Datei → alte Version sichtbar | OEK2_WILLKOMMEN_IMAGES stabile URL; getOek2WelcomeImageEffective; oek2-willkommensbild-nie-uraltbild.mdc. | BUG-020, BUG-022 |
| Variable vor Verwendung (Hooks) | useLocation/useNavigate in useEffect, Deklaration weiter unten | Router-Hooks am Komponentenanfang; variable-vor-verwendung-hooks.mdc. | ANALYSE-OEK2-GALERIE-BETRETEN-FEHLER-06-03 |
| Mobil: Freistellen/Vollkachel | Auf Mobil angeboten → Absturz / unnötige Last | isMobileDevice; showFreistellen={!isMobileDevice}; runBildUebernehmen erzwingt „original“. mobile-freistellen-vollkachel-nie.mdc. | Mehrfach (10.03.26) |
| Bereinigung: Merge/Reload holt Bilder zurück | Nach „Bilder 0030–0039 bereinigen“ bleiben Bilder sichtbar (Merge behält Server-URL; Reload aus Supabase überschreibt) | preserveLocalImageData: bei lokal leerem Bild Merged-Item ohne Bild. Nach Bereinigung Event mit fromBereinigung; Galerie lädt nur aus localStorage. | BUG-024 (10.03.26) |
| Bereinigung: Admin zeigt wieder Bild (Fallback) | Nach Bereinigung zeigten Admin-Werkkarten weiterhin Fotos („dutzende Versuche“). | Admin lastSavedArtworkImageRef: (1) Bei fromBereinigung Ref auf null; (2) Fallback-Bild nie für Nummern 30–39 einsetzen; (3) Nach Klick Bereinigen Ref sofort null setzen. | BUG-024 Ergänzung (10.03.26) |
| Galerie: Fallback nur bei imageRef | Werke ohne imageRef (v. a. Keramik nach Merge) zeigten in Galerie „Kein Bild“, obwohl Dateien in public/img/k2/ (werk-K2-K-xxxx.jpg) existieren. | resolveArtworkImages: catch + else mit Vercel-Fallback; Fallback-URL auch aus number/id (getVercelFallbackIdFromArtwork). loadArtworksResolvedForDisplay: Fallback aus imageRef und aus number/id. | BUG-025 (10.03.26); FEHLERANALYSE-10-03-26-GALERIE-BILDER-STAND.md |
| **Datenmenge / Komprimierung / Speicher** | Große Datenmengen (z. B. 70 Werke mit Base64), „Laden geht nicht“, Speicher voll, Timeout. Komprimierung war schon einmal gelöst, könnte wieder aufgeweicht worden sein. | **Bei jeder Fehlermeldung zu Speicher/Laden/Komprimierung:** GELOESTE-BUGS (BUG-001, BUG-010, BUG-021) + komprimierung-fotos-videos.mdc prüfen. Beim **Speichern** von Server-Daten: niemals Base64 in localStorage schreiben – stripBase64FromArtworks vor saveArtworks. Beim **Erzeugen** von Bildern: immer compressImageForStorage (context artwork/mobile). Export: artworksForExport entfernt Base64; Bild-URLs über resolveArtworkImageUrlsForExport. | BUG-001, BUG-010, BUG-021; komprimierung-fotos-videos.mdc; qualitaet-bei-fehlermeldung.mdc |
| **Admin: Bild bei Werk A verschwindet beim Speichern von Werk B** | ~~Wiederholung~~ **✅ GELÖST 12.03.26 (BUG-033):** iframe data:→blob überall (Initial-Load, artworks-updated, Save-Pfad); Liste blob:-URLs nicht mehr verwerfen. Siehe ANALYSE-ADMIN-BILD-VERSCHWINDET-BEI-SPEICHERN.md Abschnitt 10, GELOESTE-BUGS.md BUG-033. | Bei erneutem Bildverlust in Admin iframe: BUG-033 + Analyse-Dokument Abschnitt 10 prüfen; blob-Liste und convertDataUrlsToBlobUrlsInList nicht zurückbauen. | BUG-033 (12.03.26) |
| **Link zu Projekt-Unterseite öffnet APf (Wiederholung 10×)** | **✅ GELÖST 12.03.26 (BUG-034):** Route-Reihenfolge (spezifische Routen vor `/projects/:projectId`); Catch-all zeigt bei Unterseiten-URL Hinweis statt stiller Redirect; Doku + Regel + Checkliste. Siehe ANALYSE-LINK-OEFFNET-APF-STATT-UNTERSEITE.md, link-projekt-unterseite-nie-apf.mdc. | Bei neuer Projekt-Unterseite: Route in App.tsx **vor** `/projects/:projectId`; Link mit BASE_APP_URL + path; bei „Link öffnet APf“: Analyse-Doku + Regel lesen. | BUG-034 (12.03.26) |
| **Design-Fehler: Autoritative Quelle / Sync** | Merge ließ Lokal den Server überschreiben („Mobile gewinnt“, „neueres updatedAt gewinnt“). Nach „An Server senden“ muss beim Abholen **Server = einzige Wahrheit** sein. Viele Stunden Frust. **Lehre:** Bei Sync/„eine Quelle der Wahrheit“ **zuerst** die Grundregel klären (wer ist nach welcher Nutzeraktion die Wahrheit?), dann bauen. | serverAsSoleTruth: true bei allen Lade-Pfaden; LEHRE-DESIGN-FEHLER-SERVER-WAHRHEIT.md; prozesssicherheit-veroeffentlichen-laden.mdc. Bei neuem Sync/Thema: Grundregel zuerst. | 13.03.26; BUG-037 |
| **Schreiben in falschen Key / falscher Kontext** | Ein Ablauf (z. B. Auto-Save) schreibt **unabhängig vom aktuellen Kontext** immer in denselben Key (z. B. K2-Keys). Wenn der Ablauf auch bei VK2/ök2 läuft, landen VK2/ök2-Daten in K2 → K2-Galerie zeigt fremde Inhalte. **Lehre:** Zuerst Struktur und Datenfluss (wer schreibt wohin, unter welcher Bedingung?), dann an der **Quelle** fixen (z. B. Auto-Save nur bei K2), nicht nur an der Anzeige bereinigen. | fehlersuche-zuerst-struktur-datenfluss.mdc; DATENFLUSS-GALERIE-STRUKTUR.md; Auto-Save nur bei K2 (ScreenshotExportAdmin: !tenant.isOeffentlich && !tenant.isVk2). Bei „falsche Daten angezeigt“: Schreibpfad prüfen. | BUG-039 (15.03.26) |

*(Wird bei neuen Fehlerklassen und Wiederholungen ergänzt.)*

---

## Laufende Absicherungen (Checklisten / Regeln)

- **K2 Familie – Lehren aus K2 Galerie:** docs/K2-FAMILIE-LEHREN-AUS-K2-GALERIE.md; .cursor/rules/k2-familie-lehren-galeria-anwenden.mdc. Bei jeder Änderung an K2 Familie und bei Fehlermeldungen zu K2 Familie: diese Lehren anwenden, damit dieselben Fehlerklassen nicht ein zweites Mal auftreten.
- **Veröffentlichen & Laden:** docs/PROZESS-VEROEFFENTLICHEN-LADEN.md; .cursor/rules/prozesssicherheit-veroeffentlichen-laden.mdc.
- **Vor „Fertig“ bei Sync/Publish/Load:** Kein stilles Überschreiben; Nutzer sieht immer Erfolg oder Fehler; eine Quelle für Senden und Laden.
- **Qualitätsprozess:** .cursor/rules/qualitaet-bei-fehlermeldung.mdc (bei Fehlermeldung: Protokoll lesen → analysieren → fixen + absichern → hier eintragen).
- **Hinweis für Georg:** Dieses Protokoll ist für die KI verbindlich – sie orientiert sich daran und muss es bei Fehlern aktualisieren. Für dich entsteht keine Mehrarbeit.

---

## Protokoll-Einträge

*(Neueste zuerst. Kurz: Datum, Stichwort, Ursache, Maßnahme.)*

- **18.03.26 – Telefonnummer springt immer wieder auf alten Wert (Stammdaten):** Georg: Nummer schon mindestens 10× richtig eingegeben, nach einiger Zeit kommt wieder die falsche. **Ursache:** In handleRefresh („Bilder vom Server laden“ / Stand-Badge) hatten **Server-Daten Vorrang** vor dem bestehenden State (prev). Enthielt gallery-data.json die alte Nummer, wurde sie bei jedem Refresh in den State geschrieben und erschien im Impressum. **Maßnahme:** GaleriePage handleRefresh – bei Martina/Georg/Galerie-Kontaktdaten (name, email, phone, website, address, city, country) gilt jetzt **prev zuerst**; Server nur, wenn prev leer. DATENFLUSS-GALERIE-STRUKTUR.md Punkt 6 ergänzt.
- **16.03.26 – K2 zeigte weiterhin VK2-Inhalte (BUG-039 Nachschärfung):** Georg: Problem besteht noch (Stand 13.02). **Ursachen:** (1) URL-Parameter `?context=VK2` (großgeschrieben) wurde nicht erkannt → deriveTenantId fiel auf getTenantFromStorage() → evtl. tenantId = 'k2' → Auto-Save lief mit VK2-State. (2) Keine zweite Absicherung, falls tenant einmal falsch. **Maßnahme:** (1) TenantContext: context-Wert aus URL und bei syncStorageFromUrl **case-insensitive** (toLowerCase().trim()). (2) ScreenshotExportAdmin: Auto-Save nur starten, wenn zusätzlich die URL weder `context=oeffentlich` noch `context=vk2` enthält (location.search, case-insensitive). GELOESTE-BUGS BUG-039 ergänzt.
- **13.03.26 – Design-Fehler Sync / „Server = einzige Wahrheit“ (Lehre für alle ähnlichen Probleme):** Merge ließ Lokal den Server überschreiben (Mobile/neueres gewinnt). Richtige Regel von Anbeginn: Nach „An Server senden“ ist beim Abholen Server die einzige Wahrheit – Mac und Handy müssen zu 100 % gleich sein. **Maßnahme:** (1) serverAsSoleTruth in syncMerge.ts, alle Lade-Pfade nutzen es. (2) docs/LEHRE-DESIGN-FEHLER-SERVER-WAHRHEIT.md: Was schief war, warum, Lehre für Sync/authoritative source (Grundregel zuerst klären, dann bauen). (3) FEHLERANALYSEPROTOKOLL: Quelle + Fehlerklasse „Design-Fehler: Autoritative Quelle“. (4) GELOESTE-BUGS BUG-037. Damit so etwas nie wieder passiert – auch bei anderen Problemen.
- **13.03.26 – ök2 zwei Werkbilder fehlen / alte Bilder Handy – akzeptierte Limitation (keine weiteren Versuche):** Georg: Mit den Werkbildern im ök2 (zwei fehlen) und den alten Bildern am Handy muss er sich abfinden; dutzende Male getestet, nichts hat funktioniert. „Weist du keine Lösung sondern probierst halt vielleicht wird es schon mal gehen – das ist absolut unprofessionell und kein Sportwagenmodus, das machen Dilettanten so und nicht die Profis.“ **Maßnahme:** docs/BEKANNTE-LIMITATIONEN-AKZEPTIERT.md angelegt: beide Punkte als akzeptierte Limitationen; **keine** weiteren „vielleicht geht’s“-Versuche. Nur bei neuer konkreter Hypothese oder neuem Mittel mit Georg klären. FEHLERANALYSEPROTOKOLL: Quelle „Akzeptierte Limitationen“ in Tabelle ergänzt.
- **13.03.26 – ök2 Werke verwalten: Alle Musterwerke zeigen dasselbe Bild (Vase) – BUG-035:** **Ursache:** prepareArtworksForStorage speicherte externe URLs (Unsplash) nicht als imageRef; IndexedDB-Suche für M1/K1/G1/S1 lieferte dieselbe Variante (k2-img-1) → ein Bild für alle. **Maßnahme:** (1) Externe URL (http/https) in prepareArtworksForStorage als imageRef übernehmen, keine IDB-Suche. (2) resolveArtworkImages: Musterwerke (M1, K1, G1, S1, O1, muster-*) nicht aus IndexedDB befüllen; UI nutzt getOek2DefaultArtworkImage(category). (3) GELOESTE-BUGS.md BUG-035.
- **12.03.26 – Werke anlegen/Bilder einsetzen: (1) Nach „Stand erneuern“ alle Bilder weg, (2) Meldung „Speichern nicht möglich“:** **Szenario:** Georg setzte neue Bilder ein, speicherte, klickte dann „Stand erneuern“ (Vom Server laden) → alle Bilder weg. Beim zweiten Versuch erschien das Modal „Speichern nicht möglich“. **Ursachen:** (1) „Stand erneuern“ lädt Server-Stand; wenn vorher nicht „An Server senden“ geklickt wurde, hat der Server noch alte Daten → Merge kann lokale Bilder nur behalten, wenn lokales Werk neuer ist (updatedAt) und preserveLocalImageData greift; ggf. Reihenfolge „erst senden, dann laden“ nicht eingehalten. (2) „Speichern nicht möglich“ = **An Server senden** fehlgeschlagen (nicht lokales Speichern); Modal war missverständlich. **Maßnahme:** (1) Beim **Bearbeiten** im Admin wird `artworkData.updatedAt = new Date().toISOString()` explizit gesetzt, damit Merge beim „Vom Server laden“ die lokale Version behält (neueres gewinnt). (2) Modal umbenannt in „An Server senden nicht möglich“, Text: „Deine Werke sind lokal gespeichert. Nur das Senden …“; ggf. Fehlertext (publishErrorMsg) anzeigen. (3) Hinweis bei „Vom Server laden“ (K2): „Nach neuen Bildern: zuerst ‚An Server senden‘, dann hier laden – sonst können Bilder fehlen.“ (4) Doku: Ablauf „Speichern → An Server senden → dann Stand erneuern“ in PROZESS-VEROEFFENTLICHEN-LADEN.md bereits beschrieben; Hinweis in UI ergänzt.
- **12.03.26 – Link öffnet nur APf / „10.mal wo bleibt fehleranalyse“ (Georg) – BUG-034:** **Wiederholung** derselben Fehlerklasse: Klick auf Projekt-Unterseiten-Link (z. B. Präsentationsmappe Vollversion) führt zur APf statt zur Zielseite. **Ursachen:** (1) Route-Reihenfolge – spezifische Routen müssen vor `/projects/:projectId` stehen; (2) Stiller Redirect bei nicht gematchter URL (`path="*"` → `/`) ohne Hinweis; (3) ggf. Deploy-Stand. **Maßnahme:** (1) Analyse-Dokument docs/ANALYSE-LINK-OEFFNET-APF-STATT-UNTERSEITE.md; (2) Catch-all in App.tsx durch NotFoundOrRedirect ersetzt – bei URL `/projects/k2-galerie/...` (3+ Segmente) ohne Match Hinweis „Seite nicht gefunden. Stand aktualisieren.“ statt Redirect; (3) BUG-034 in GELOESTE-BUGS; (4) Fehlerklasse „Link zu Projekt-Unterseite öffnet APf“ + Regel link-projekt-unterseite-nie-apf.mdc mit Checkliste bei neuer Unterseite.
- **12.03.26 – Admin iframe: „Hurra gelöst“ (Georg) – BUG-033:** **Ursachen:** (1) iframe strippt data:-URLs → State ohne Bild; (2) Werkkarten-Liste verwarf blob:-URLs (`rawSrc.startsWith('blob:') → rawSrc = ''`) → Liste „Kein Bild“, Bearbeiten zeigte Bild (blob im Objekt); (3) data:→blob nur im Save-Pfad. **Wo die Bugs waren:** (1) ScreenshotExportAdmin.tsx – setAllArtworksSafe-Aufrufer ohne convertDataUrlsToBlobUrlsInList (Initial-Load, artworks-updated); stripArtworkImagesForPreview machte data: → ''. (2) ScreenshotExportAdmin.tsx – Werkkarten-Liste (filtered.map): Zeile `if (rawSrc.startsWith('blob:')) rawSrc = ''`. (3) ScreenshotExportAdmin.tsx – convertDataUrlsToBlobUrlsInList nur im Save-Pfad, nicht beim ersten Laden / artworks-updated. **Maßnahme:** (1) convertDataUrlsToBlobUrlsInList in iframe überall (Initial-Load, artworks-updated, Save-Pfad). (2) Zeile in Liste entfernt, die blob:-URLs verwarf; onError zeigt bei ungültigem Blob Platzhalter. (3) BUG-033 in GELOESTE-BUGS.md, ANALYSE-ADMIN-BILD-VERSCHWINDET-BEI-SPEICHERN.md Abschnitt 10 inkl. Tabelle „Wo die Bugs waren“, Fehlerklasse „Admin Bild verschwindet“ als gelöst markiert.
- **11.03.26 – Fix „noch einmal“ (Agent):** Auf Basis der Analyse alle State-Pfade abgesichert: Favorit (toWrite = preserveStorageImageRefs vor save + setState), Import (toWrite vor save + setState), Storage-Listener (withRefs vor setState), API data.artworks beide Stellen (dynamic tenant). ANALYSE-ADMIN-BILD-VERSCHWINDET-BEI-SPEICHERN.md Abschnitt 6 aktualisiert. Tests 49/49 grün.
- **11.03.26 – Georg: „Wie oft machst du das gleiche“ / Stand 6.58 noch immer kein anderes Ergebnis (Admin: Bild verschwindet bei anderem Werk):** **Wiederholung** derselben Fehlerklasse (Bildverlust beim Speichern / imageRef geht verloren). Bisherige Fixes (resolveArtworkImages imageRef nicht leeren, preserveStorageImageRefs nach Save, Warteschlange doSerializedWrite, pauseAutoSaveForMs) reichen offenbar nicht – Georg meldet unverändertes Verhalten. **Maßnahme:** (1) **Fehleranalyseprotokoll:** Neue Fehlerklasse „Admin: Bild bei Werk A verschwindet beim Speichern von Werk B“ ergänzt; Verweis auf systematische Analyse. (2) **Analyse-Dokument:** docs/ANALYSE-ADMIN-BILD-VERSCHWINDET-BEI-SPEICHERN.md angelegt – erfasst alle Schreib- und State-Pfade, mögliche Ursachen, nächste Schritte (Reproduktion mit Logging, Favorit-Pfad absichern, alle setAllArtworksSafe prüfen). (3) **Regel für KI:** Bei diesem Thema zuerst Analyse-Dokument lesen; keinen weiteren Einzelfix ohne Systematik. Qualität: regel-nach-3-versuchen-vertiefen.mdc – vertiefte Analyse statt im Kreis drehen.
- **11.03.26 – „60 mit Bild angekommen, 59 gespeichert“ (ein Bild geht beim Laden vom Server verloren):** Georg: Nach „Aktuellen Stand holen“ Meldung „70 Werke, 60 mit Bild angekommen. Gespeichert: 70 Werke, 59 mit Bild“ – Bilder in der Galerie blieben Platzhalter. **Ursache:** In preserveLocalImageData zählte „lokale echte URL“ auch data:-URLs. Ein Werk hatte vom Server eine https-URL, lokal eine data:-URL → wir übernahmen die lokale data:-URL; stripBase64FromArtworks entfernt sie beim Speichern → ein Werk ohne Bild-URL → 59 statt 60. **Maßnahme:** localHasRealUrl nur bei echten http(s)-URLs (syncMerge.ts). Server-https wird nicht mehr durch lokale data:-URL ersetzt; nach Merge/Speichern bleiben 60 mit Bild.
- **11.03.26 – iPad: Bilder bis auf 2 weg nach Stand/Neuladen (Bildverlust):** Georg: Nach Push/Neuladen auf dem iPad fast alle Bilder weg. **Ursache (Verdacht):** (1) GaleriePage loadData: localArtworks beim ersten Lauf leer oder zu klein → Merge = vorwiegend Server (2 Werke mit Bild) → Speichern überschrieb lokale Daten. Oder (2) Re-Join-Zweig in prepareArtworksForStorage („Kette wieder am Glied“) unerwünschter Seiteneffekt. **Maßnahme:** (1) Re-Join in prepareArtworksForStorage entfernt. (2) prepareArtworksForStorage: vorhandenes imageRef **niemals** leer ausgeben (`hadRef && !next.imageRef` → next.imageRef = a.imageRef). (3) GaleriePage loadData: Vor Speichern prüfen – wenn lokal mehr Werke mit imageRef als nach Merge → **Speichern überspringen** (Schutz vor Überschreiben „70 mit Bild“ durch „2 mit Bild“). Wiederherstellung: Vollbackup oder vom Gerät mit allen Bildern erneut senden. DIALOG-STAND ergänzt.
- **11.03.26 – Brief an Andreas: weiße Seite nach „Wird geladen“:** Endlosschleife in renderBoldItalic (Rest `**` nicht konsumiert). Fix: Zeichen konsumieren; try/catch + Fehlerzustand. BUG-027.
- **10.03.26 – Analyse: „bis 39 keine Bilder, ab 40 Platzhalter“ (Georg):** **Bis 39:** Für 30–39 wird **absichtlich kein** Vercel-Repo-Fallback genutzt (STATIC_FALLBACK_EXCLUDE_RANGE), damit alte gelöschte Repo-Dateien nicht wieder erscheinen. Fehlt die echte Bild-URL (Server/IndexedDB), bleibt nur leer oder „Kein Bild“. **Ab 40:** Für 40–48 baut die App eine Fallback-URL (werk-K2-K-0040.jpg usw.). Im Repo existieren diese Dateien nicht (nur z. B. werk-K2-K-0030/0031). Browser lädt URL → 404 → Bild fehlgeschlagen → **Platzhalter** („Kein Bild“-Div). **Kern:** Echte Bilddaten für 30–48 fehlen (verloren beim Publish/Load). Unterschied nur in der Anzeige: 30–39 = kein Fallback versucht; 40–48 = Fallback versucht, 404 → Platzhalter. **Wiederherstellung:** Nur über Gerät/Backup, das noch Bilddaten hat, dann Veröffentlichen; „Werke vom Server zurückholen“ überschreibt lokale Bilder nicht mehr (Fix 10.03.26).
- **10.03.26 – „Werke vom Server zurückholen“ überschrieb lokale Bilder (30–48):** Georg: iPad Senden 19:25, Mac Laden 19:25 – Bilder 30–48 fehlen weiterhin. **Zusätzliche Ursache:** Der Admin-Button **„Werke vom Server zurückholen“** (Einstellungen) ersetzte lokal **komplett** durch die Server-Liste **ohne** preserveLocalImageData. Wenn der Server 30–48 ohne imageUrl hatte (z. B. nach früherem Publish ohne Auflösung), wurden lokale Bilddaten dabei gelöscht. **Maßnahme:** handleRestoreWerkeFromPublished nutzt jetzt **preserveLocalImageData(serverList, localArtworks)** vor dem Speichern – Server-Stand als Basis, lokale Bilddaten (imageUrl/imageRef) bleiben erhalten. Hinweis in Erfolgsmeldung: „lokale Bilder beibehalten“. Regel: „Merge ohne preserveLocalImageData“ (bereits in Bekannte Fehlerklassen).
- **10.03.26 – Bilder 30–48 am Mac nach iPad-Export nicht angekommen:** Georg: Bilder 30–48 vom letzten iPad-Export sind am Mac noch nicht da. **Ursache:** Im Admin wurde beim Veröffentlichen die **Rohliste** (readArtworksRawByKey) an publishGalleryDataToServer übergeben – also nur imageRef, keine aufgelösten Bild-URLs. resolveArtworkImageUrlsForExport löst nur auf dem **publizierenden** Gerät (IndexedDB). Wenn der Mac die Bilder 30–48 nicht in IndexedDB hatte (weil sie nur vom iPad kamen), blieben die URLs leer → Server speicherte 30–48 ohne Bild-URL → Mac bekam sie beim „Aktuellen Stand holen“ nicht. **Maßnahme:** Alle Admin-Publish-Stellen nutzen vor dem Veröffentlichen **resolveArtworkImages** (aufgelöste Liste): (1) Hauptablauf K2-Veröffentlichen: toPublish = await resolveArtworkImages(raw). (2) Beide Bereinigung-Buttons (30–39 „Überall“): toPublishResolved = await resolveArtworkImages(readArtworksRawByKey('k2-artworks')); publishGalleryDataToServer(toPublishResolved). So gehen bestehende URLs (z. B. vom Server) und IndexedDB-Bilder mit. Ein Standard wie GalerieVorschauPage (readArtworksForContextWithResolvedImages vor Publish).
- **10.03.26 – Datenmengen/Speicher, 70 Werke laden, Fehleranalyseagent:** Georg: „70 Bilder reinladen geht nicht“, „Vermutung Problem mit Datenmengen, Komprimierung war schon gelöst, vielleicht wieder aufgehoben“, „wo ist unser Fehleranalyseagent, warum ist der noch nicht aktiv“. **Ursache:** (1) Fehleranalyseablauf (qualitaet-bei-fehlermeldung.mdc) wurde bei der Meldung nicht konsequent ausgeführt (GELOESTE-BUGS + Protokoll zuerst). (2) Beim Speichern von Server-Daten („Vom Server laden“) konnte theoretisch Base64 in die Antwort geraten (alter Stand, andere Quelle) → 70 große Bilder in localStorage = Quota/Speicher/Timeout. **Maßnahme:** Fehlerklasse „Datenmenge / Komprimierung / Speicher“ im Protokoll ergänzt; Absicherung: Server-Artworks vor dem Schreiben in localStorage von Base64 befreien (stripBase64FromArtworks), damit nie versehentlich große Datenmengen geschrieben werden. Fehleranalyseagent = KI folgt bei jeder Fehlermeldung verbindlich qualitaet-bei-fehlermeldung.mdc (Protokoll + GELOESTE-BUGS zuerst).
- **10.03.26 – Galerie: viele Keramik-Bilder fehlen, Stand mobil (~5 h Fehlersuche):** (1) **Bilder in Galerie fehlen** trotz Bild in Werken (z. B. K2-K-0013/0014): resolveArtworkImages im catch pushte Werk ohne imageUrl; loadArtworksResolvedForDisplay hatte Fallback nur bei imageRef. (2) **Viele Keramik ohne Bild:** Werke ohne imageRef bekamen nie Vercel-URL (Dateien werk-K2-K-xxxx.jpg liegen in public/img/k2/). **Maßnahme:** Fallback in catch; Fallback aus **number/id** (getVercelFallbackIdFromArtwork) in resolveArtworkImages (else) und in loadArtworksResolvedForDisplay. BUG-025; ausführlich docs/FEHLERANALYSE-10-03-26-GALERIE-BILDER-STAND.md. Stand mobil am Ende korrekt (bestehende QR/API-Regeln).
- **10.03.26 – „Bilder 0030–0039 bereinigen“ – Admin zeigt wieder Bild (Fallback):** Trotz BUG-024 blieben Fotos „nach dutzenden Versuchen“ sichtbar. **Ursache:** Admin lastSavedArtworkImageRef setzte für Werke ohne/kleines imageUrl ein Fallback-Bild → bereinigte Werke 30–39 bekamen wieder ein Bild. **Maßnahme:** Bei fromBereinigung Ref null; Fallback nie für Nummern 30–39; im Bereinigen-Button Ref sofort null. Siehe BUG-024 Ergänzung.
- **10.03.26 – „Bilder 0030–0039 bereinigen“ – Bilder bleiben sichtbar:** Nach Bereinigung (lokal, IndexedDB, Supabase, GitHub) zeigte die Galerie die Bilder weiterhin. **Ursache:** (1) Beim Merge (z. B. „Vom Server laden“) blieb bei lokal leerem Bild die Server-URL erhalten (preserveLocalImageData: `if (!localHasImage) return item`). (2) Nach Dispatch lud die Galerie aus Supabase → Race oder alter Supabase-Stand konnte Anzeige überschreiben. **Maßnahme:** (1) preserveLocalImageData: Wenn lokal kein Bild (bewusst leer nach Bereinigung), Merged-Item ebenfalls ohne Bild setzen (`imageUrl/imageRef/previewUrl` leer). (2) Nach Bereinigung Event mit `detail: { fromBereinigung: true }`; GalerieVorschauPage bei fromBereinigung nur aus localStorage laden (kein Supabase), damit Anzeige sofort den bereinigten Stand zeigt. Protokoll ergänzt; siehe BUG-024.
- **10.03.26 – Sync Mobil→Mac „kommt nichts an“:** (1) Veröffentlichen ging teils an andere Quelle als Lade-URL. (2) Bei API-Fehler wurde statische Datei geladen und überschrieb mit altem Stand. (3) Kein sichtbares Feedback bei Veröffentlichen. **Maßnahme:** Immer Vercel-URL für POST; bei API-Fehler keine statische Datei, Fehlermeldung; setLoadStatus für Veröffentlichen-Erfolg/-Fehler. Protokoll und Regel angelegt.

---

*Dieses Protokoll wird bei jeder Fehlermeldung von Georg konsultiert und nach jeder Analyse aktualisiert.*
