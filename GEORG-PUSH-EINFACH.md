# Georg – Einfach pushen

## Ein Befehl – kopieren und im Terminal einfügen

**Im Terminal am Mac** (oder Cursor Terminal):

1. Hierhin gehen:
   ```
   cd /Users/georgkreinecker/k2Galerie
   ```

2. Dann diesen Befehl (alles in eine Zeile):
   ```
   git add . && git status && git commit -m "Session 13.02.26: Sync, Mobile, ök2" && git push origin main
   ```
   (git add . = alle Änderungen; git status = zur Kontrolle vor dem Commit)

3. Fertig.

---

Wenn „Everything up-to-date“ erscheint = alles ist schon aktuell auf GitHub.

Wenn eine Fehlermeldung kommt = den ganzen Text kopieren und an mich schicken.
