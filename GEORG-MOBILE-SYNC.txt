═══════════════════════════════════════════════════════════════
  📱 Mobile-Synchronisation - So funktioniert es
═══════════════════════════════════════════════════════════════

GEORG:

So funktioniert die Mobile-Synchronisation:

═══════════════════════════════════════════════════════════════

WIE ES FUNKTIONIERT:

1. Werk speichern auf Mac
   → Wird in localStorage gespeichert ✅
   → Event "artwork-saved-needs-publish" wird dispatched ✅

2. Automatisches Veröffentlichen
   → DevViewPage hört auf Event ✅
   → Ruft automatisch publishMobile() auf ✅
   → Schreibt Werke in public/gallery-data.json ✅

3. Mobile-Gerät scannt QR-Code
   → Öffnet Galerie-Seite ✅
   → Lädt Werke aus gallery-data.json ✅
   → Merged mit eigenem localStorage ✅

═══════════════════════════════════════════════════════════════

WICHTIG FÜR MOBILE-SYNC:

✅ Dev-Server muss laufen (npm run dev)
✅ Mac und Mobile müssen im gleichen Netzwerk sein
✅ QR-Code muss auf die richtige URL zeigen (z.B. http://192.168.x.x:5177)

═══════════════════════════════════════════════════════════════

WAS PASSIERT BEIM SPEICHERN:

1. Werk wird gespeichert → localStorage auf Mac ✅
2. Event wird dispatched → "artwork-saved-needs-publish" ✅
3. DevViewPage hört Event → ruft publishMobile() auf ✅
4. publishMobile() schreibt → public/gallery-data.json ✅
5. Mobile-Gerät lädt Datei → sieht neue Werke ✅

═══════════════════════════════════════════════════════════════

PROBLEM-LÖSUNG:

Wenn Mobile-Geräte die neuen Werke nicht sehen:

1. Prüfe ob Dev-Server läuft (npm run dev)
2. Prüfe ob QR-Code auf richtige URL zeigt
3. Prüfe ob Mac und Mobile im gleichen Netzwerk sind
4. Mobile-Gerät: Seite neu laden oder QR-Code neu scannen

═══════════════════════════════════════════════════════════════

ZUSAMMENFASSUNG:

Die Synchronisation funktioniert automatisch:
→ Werk speichern → automatisch veröffentlicht → Mobile sieht es

Wichtig: Dev-Server muss laufen und beide Geräte müssen
im gleichen Netzwerk sein!

═══════════════════════════════════════════════════════════════
