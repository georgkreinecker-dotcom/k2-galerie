═══════════════════════════════════════════════════════════════
  🔥 VERCEL 404-FEHLER - Lösung
═══════════════════════════════════════════════════════════════

GEORG:

Das Problem: gallery-data.json fehlt auf Vercel (404-Fehler)

═══════════════════════════════════════════════════════════════

DAS PROBLEM:

1. Werk wird gespeichert → localStorage ✅
2. Automatische Veröffentlichung → gallery-data.json wird lokal geschrieben ✅
3. ABER: Datei muss zu Git gepusht werden → sonst nicht auf Vercel ❌
4. Mobile-Gerät scannt QR-Code → 404-Fehler weil Datei fehlt ❌

═══════════════════════════════════════════════════════════════

DIE LÖSUNG:

Nach dem automatischen Veröffentlichen wird jetzt ein Event
dispatched das Git Push auslöst.

ABER: Browser kann nicht direkt Git Push ausführen!

═══════════════════════════════════════════════════════════════

WAS DU MACHEN MUSST:

1. Werk speichern → wird automatisch veröffentlicht ✅
2. Terminal öffnen (am Mac)
3. Führe aus:
   cd /Users/georgkreinecker/k2Galerie
   bash scripts/git-push-gallery-data.sh

ODER:

1. In DevViewPage: Button "📦 Git Push" klicken
2. Warte 1-2 Minuten auf Vercel Deployment
3. Mobile: QR-Code neu scannen

═══════════════════════════════════════════════════════════════

WICHTIG:

Die Datei wird lokal geschrieben, aber sie muss zu Git gepusht
werden damit Vercel sie deployt!

Ohne Git Push → 404-Fehler auf Vercel

═══════════════════════════════════════════════════════════════

ZUSAMMENFASSUNG:

Problem: gallery-data.json fehlt auf Vercel
Lösung: Nach Veröffentlichung Git Push ausführen
Weg: Terminal oder "📦 Git Push" Button

═══════════════════════════════════════════════════════════════
