═══════════════════════════════════════════════════════════════
  🔥 VERCEL ERROR - Lösung
═══════════════════════════════════════════════════════════════

GEORG:

Das Problem: Vercel Deployment zeigt "Error" Status

═══════════════════════════════════════════════════════════════

MÖGLICHE URSACHEN:

1. TypeScript-Fehler beim Build
2. gallery-data.json fehlt oder ist leer
3. Build-Prozess schlägt fehl

═══════════════════════════════════════════════════════════════

LÖSUNG SCHRITT FÜR SCHRITT:

1. PRÜFE LOKALEN BUILD:
   
   Terminal öffnen:
   cd /Users/georgkreinecker/k2Galerie
   npm run build
   
   Falls Fehler → diese zuerst beheben!

2. PRÜFE OB DATEI EXISTIERT:
   
   ls -la public/gallery-data.json
   
   Falls nicht vorhanden → Werk speichern und veröffentlichen!

3. PRÜFE OB DATEI WERKE ENTHÄLT:
   
   node -e "const fs=require('fs'); const d=JSON.parse(fs.readFileSync('public/gallery-data.json')); console.log('Werke:', d.artworks?.length || 0)"

4. GIT PUSH AUSFÜHREN:
   
   bash scripts/git-push-gallery-data.sh
   
   ODER manuell:
   git add public/gallery-data.json
   git commit -m "Update gallery-data.json"
   git push origin main

5. PRÜFE VERCEL DASHBOARD:
   
   - Gehe zu vercel.com/dashboard
   - Klicke auf das fehlgeschlagene Deployment
   - Prüfe die Build-Logs
   - Kopiere Fehlermeldung falls vorhanden

═══════════════════════════════════════════════════════════════

WICHTIG:

- Lokaler Build muss erfolgreich sein bevor Push zu Vercel!
- gallery-data.json muss existieren und Werke enthalten!
- TypeScript-Fehler blockieren Vercel Build!

═══════════════════════════════════════════════════════════════
