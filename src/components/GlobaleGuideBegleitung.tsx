/**
 * **Abgeschaltet (März 2026):** Der schwarze globale Vollbild-Guide wird nicht mehr gerendert.
 *
 * **Wichtig:** Speicher und Typen für `k2-guide-flow` liegen **nicht** hier, sondern in
 * **`src/utils/k2GuideFlowStorage.ts`** (grüner Admin-Balken ök2/VK2, GalerieEntdeckenGuide-Cleanup, …).
 *
 * Diese Datei bleibt nur als **leerer Stub**, falls noch ein dynamischer Import existiert –
 * **nicht** wieder mit UI füllen ohne ausdrückliche Anordnung.
 */
export function GlobaleGuideBegleitung() {
  return null
}
